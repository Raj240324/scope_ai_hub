VITE_TAWK_PROPERTY_ID=
VITE_TAWK_WIDGET_ID=

BREVO_API_KEY=updated production api key

VITE_SENTRY_DSN=

ALLOWED_HOSTNAME=scope-ai-hub.vercel.app
# Supabase (server-side only — NEVER expose service role key)
SUPABASE_URL=updated prod ready url

SUPABASE_SERVICE_ROLE_KEY=updated prod ready role key

RECAPTCHA_SECRET_KEY=updated prod secret key in vercel

# Health endpoint bearer token (optional — without it, /api/health returns minimal response)
HEALTH_CHECK_TOKEN=

ALLOWED_ORIGINS=http://localhost:5173,https://scope-ai-hub.vercel.app,https://scopeaihub.com